---
import AppLayout from "@/layouts/AppLayout.astro";
import { BatchForm } from "@/components/batches/BatchForm";
import { createClient } from "@/lib/supabase";
import type { Batch } from "@/types";

const { id } = Astro.params;
const supabase = createClient(Astro.request.headers, Astro.cookies);

if (!supabase || !id) {
  return Astro.redirect("/batches");
}

// eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
// eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
const { data, error } = await supabase.from("batches").select("*").eq("id", id).single();

if (error || !data) {
  return new Response(null, { status: 404 });
}

const batch = data as Batch;
---

<AppLayout title={batch.name}>
  <div class="mb-8">
    <a href="/batches" class="text-sm text-muted-foreground hover:text-foreground">← My Batches</a>
    <h1 class="mt-2 text-2xl font-bold">{batch.name}</h1>
  </div>

  <BatchForm client:load mode="edit" initialData={batch} />

  <div class="mt-8 border-t pt-6">
    <button
      type="button"
      disabled
      title="Coming soon"
      class="cursor-not-allowed rounded-md border border-destructive/30 px-4 py-2 text-sm font-medium text-destructive/50 opacity-50"
    >
      Delete Batch
    </button>
  </div>
</AppLayout>
